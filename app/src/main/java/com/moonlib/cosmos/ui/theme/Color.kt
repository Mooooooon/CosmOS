package com.moonlib.cosmos.ui.theme

import androidx.compose.ui.graphics.Color

// ── 深空色板 ────────────────────────────────────────────────────
// 背景层
val SpaceDeepBlack = Color(0xFF05070F)
val SpaceNavy      = Color(0xFF0B1020)
val SpaceIndigo    = Color(0xFF0D1535)

// 主色调 — 星云紫青
val NebulaPurple   = Color(0xFF8B5CF6)
val NebulaCyan     = Color(0xFF22D3EE)
val NebulaBlue     = Color(0xFF3B82F6)

// 强调 / 高光
val GlowPurple     = Color(0xFFB388FF)
val GlowCyan       = Color(0xFF67E8F9)
val StarWhite      = Color(0xFFF0F4FF)

// 表面 / 卡片 (半透明，搭配 alpha 使用)
val SurfaceDark    = Color(0xFF111827)
val SurfaceCard    = Color(0xFF1A2540)
val SurfaceBorder  = Color(0xFF2A3A5C)

// 状态栏虚拟图标色
val StatusIconColor = Color(0xCCE2E8FF)    // 80% 白
val StatusBarBg     = Color(0x66000000)    // 40% 黑毛玻璃

// ── 优雅浅色色板 ────────────────────────────────────────────────────
// 背景与容器
val LightBackground     = Color(0xFFF5F7FB)    // 优雅浅紫蓝
val LightSurface        = Color(0xFFFFFFFF)    // 纯白表面
val LightSurfaceCard    = Color(0xFFEAEFF8)    // 浅卡片灰蓝
val LightSurfaceBorder  = Color(0xFFD8E1F0)    // 浅边框线

// 状态栏虚拟图标色 (浅色)
val LightStatusIconColor = Color(0xCC1F2937)   // 80% 暗灰
val LightStatusBarBg     = Color(0x99FFFFFF)   // 60% 白毛玻璃

// 文本与前景 (浅色)
val LightTextPrimary    = Color(0xFF1E2235)    // 极深靛蓝 (主文本)
val LightTextSecondary  = Color(0xFF6B7280)    // 中灰 (副文本)